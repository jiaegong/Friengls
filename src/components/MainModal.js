import React from 'react';

const MainModal = (props) => {
  return (
    <div>
      {/* <img src={props.userProfile}/>
      <p>{props.userName}</p>
      <p>한 줄 소개</p>
      <p>{props.language}</p>
      <p>{props.tag}</p> */}
    </div>
  );
};

export default MainModal;
